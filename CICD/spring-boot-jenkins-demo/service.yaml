apiVersion: v1
kind: Service
metadata:
  name: springboot-demo-service
  labels:
    app: springboot-demo 
spec:
  selector:
    app: springboot-demo

  ports:
    - port: 8080
      targetPort: 8080

  type: LoadBalancer
