# Allow replication connections from any standby
host replication all 0.0.0.0/0 trust

# Allow all connections (you can tighten this later)
host all all 0.0.0.0/0 trust

# Local connections (default)
local   all             all                                     trust
