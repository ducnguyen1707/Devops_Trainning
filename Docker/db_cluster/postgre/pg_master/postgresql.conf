# Listen on all network interfaces
listen_addresses = '*'

# Enable WAL for replication
wal_level = replica

# Allow multiple standby nodes to connect
max_wal_senders = 10

# Keep WAL files for standby to catch up
wal_keep_size = 64

# Logging settings (optional but helpful)
log_timezone = 'UTC'
timezone = 'UTC'
